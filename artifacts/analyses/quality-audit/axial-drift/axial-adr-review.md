# axial-adr-review — skipped

No ADR with `axial: true` frontmatter under `docs/adr/`. Playbook Wave 1 semantic drift agent is N/A.

Inferred primary axis (not an ADR): semantic plane A / B / C.

N×M siblings for later architecture/synthesis notes:

- language analyzers (`ts-analyzer` vs `python-analyzer`)
- transports (`apps/cli` vs `packages/mcp-server`)
- plugin hosts (`plugins/claude-code` vs `plugins/semctx-control`) besides `plugins/shared`
