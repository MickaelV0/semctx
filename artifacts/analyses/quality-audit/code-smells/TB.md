# Code Smells — TB

## Summary
Inventoried all 15 Plane B + `core` test files (`packages/core`, `semantic-model`, `semantic-dsl`, `semantic-engine`; 3032 loc). `core`, `path-safety`, `parse-fixes`, and `handoff-robust` are small and use helpers. No `beforeEach` exists, and no single `it` exceeds 80 loc. Worst issue is `semantic-engine/test/anchor-migration.test.ts` (1131 loc) — the only 500+ line test and the only file over 400 — which then copy-pastes spawn/race scripts and crash-recovery loops that the file already has helpers for. Next is the same `danger`/`inv:x` graph and empty `VerifyReport` restated in three engine tests, and empty `SemanticNode`/`ChangeContract` literals inlined across `model.test.ts` instead of a `node()`/`change()` helper (which `anchor-doctrine.test.ts` already has).

## Findings

| ID | Sev | Location | Issue | Evidence | Fix | Confidence |
|----|-----|----------|-------|----------|-----|------------|
| SMELL-TB-01 | P2 | packages/semantic-engine/test/anchor-migration.test.ts:1 | God test file (1131 loc). Nine `describe` suites share one module: nominal rewrite, mixed endings, preimage, generation, authority, quarantine, injected-fault atomicity (~110 loc), crash recovery (~195 loc), exclusive ownership (~356 loc). Helpers at the top are real; the file is still one 1.1k-loc test. Only TB file over 400 loc; only 500+ line test. No `it` in it is itself >80 loc. | `describe("exclusive transaction ownership and durability faults", () => {` | Split along the existing suite names: rewrite/bytes, planning refusals, atomicity+fs port, crash recovery, exclusive ownership. Keep `repository` / `invariantFile` / `crashMigration` in a local helper module, not a cross-plane fixture package. | 96 |
| SMELL-TB-02 | P2 | packages/semantic-engine/test/anchor-migration.test.ts:777 | Race helpers inlined. `crashMigration` / `crashRecovery` already wrap `Bun.spawnSync` + `-e` (113, 133). Exclusive-ownership then re-inlines the same spawn (`process.execPath`, `-e`, `stdout: "pipe"`) three times: acquire race (777), Worker recover (821), recovery race (867). Acquire and recovery races are the same `launch()` / start-file / `exits [0, 17]` / `TRANSACTION_ALREADY_ACTIVE` script with a different import. | `const launch = () => Bun.spawn([process.execPath, "-e", source, root, start], {` | One `racePair({ source, env, start })` next to `crashMigration`. Worker test keeps its `Worker` file; it should still reuse the node/facts/authority payload those scripts paste. | 94 |
| SMELL-TB-03 | P2 | packages/semantic-engine/test/anchor-migration.test.ts:643 | Crash-recovery loops and two-file repos copied. Two `for (const point of ["after-rollback-started", "after-restore", "after-restored", "after-rolled-back"])` blocks (643, 658) share setup/asserts; the second only appends a torn journal line. `twoFiles()` (405) and `twoCrashFiles()` (582) are the same `invariant.a`/`invariant.b` pair. Atomicity tests 414 and 440 clone the before/after byte asserts. Hostile-path `it`s (729) clone `twoCrashFiles` + `rewritePreparedPath`. Journal PREPARED parse at 719 repeats `rewritePreparedPath` (146). | `for (const point of ["after-rollback-started", "after-restore", "after-restored", "after-rolled-back"] as const) {` | One `twoFileRepo()`; table-drive crash points with optional `{ tornJournal, hostilePath }`. Reuse `rewritePreparedPath` (or a `readPrepared()`) instead of a second JSON.parse walk. | 93 |
| SMELL-TB-04 | P2 | packages/semantic-engine/test/engine.test.ts:23 | Copy-paste `facts()` graph across three files. `engine.test.ts:23`, `determinism.test.ts:6`, `verify-honesty.test.ts:8` all build `sym:function:x.ts:danger:5` + `inv:x` + `constrained_by` `e1`. Engine adds a test node and `endLine: 10`; the other two are near-copies. Empty `VerifyReport` (`schemaVersion: 1`, `base: null`, `head: "HEAD"`, empty arrays) is restated in `report()` / `warnReport()` / the `const report` in determinism. `model()` node lists in engine vs determinism share `goal.g` / `proof.p` / `change.c`. | `function facts(): RepositoryFacts {` | One `semantic-engine/test/helpers.ts` with `facts()`, `report(verdict)`, `model(overrides)`. `verify-honesty` and `determinism` import it; do not publish it from the package. | 95 |
| SMELL-TB-05 | P2 | packages/semantic-model/test/link-diagnostics.test.ts:19 | Identical `symbol()` helper copied into the migration test. Body is `{ id, kind, name, filePath, evidence: [], tags: [], metadata: {} }` at `link-diagnostics.test.ts:19` and `anchor-migration.test.ts:43`. Both bind `RUN` to `sym:function:src/a.ts:run`. Doctrine rebuilds that node at 47 and again inline at 170–171 instead of calling `symbol()`. | `function symbol(id: string, name: string, kind: RepositoryNode["kind"], filePath: string): RepositoryNode {` | Keep a file-local `symbol()` in each package that needs it (resolver vs migration vs doctrine). Doctrine should reuse its graph node instead of pasting it. Do not add a Plane-B-wide test fixture package for three lines. | 90 |
| SMELL-TB-06 | P2 | packages/semantic-model/test/model.test.ts:70 | Empty `SemanticNode` / `ChangeContract` literals inlined N times. Same `sourceRefs: [], repositoryLinks: [], relations: [], tags: []` (nodes) and `serves: [], preserves: [], requiresEvidence: [], openUnknowns: [], repositoryLinks: [], tags: []` (changes) at 70–75, 90, 114–126, 181–191, 197–209, 215–238. `anchor-doctrine.test.ts` already has `node()` / `change()` (16, 30). Engine tests repeat the same node soup (`engine.test.ts:57`, `determinism.test.ts:19`, `verify-honesty.test.ts:37`). | `{ id: "goal.b", kind: "goal", statement: "B", status: "declared", provenance: "author", sourceRefs: [], repositoryLinks: [], relations: [], tags: [] },` | Local `node({ id, kind, ... })` / `change({ id, ... })` in `model.test.ts`, same shape as doctrine. Engine side is SMELL-TB-04. | 92 |
| SMELL-TB-07 | P2 | packages/semantic-engine/test/engine.test.ts:188 | Gitignore policy string inlined three times, and `engine.test.ts` (293 loc) mixes six concerns (links, slice, contract lifecycle, four verdicts, handoff, gitignore, target binding). The project-only policy (`node_modules/` / `.semctx/*` / `!.semctx/semantic/` / `project/**` / `config.json`) is pasted at 188–196, 216–225, 243–251. | `"!.semctx/semantic/project/**",` | `const PROJECT_ONLY = [...]` once. Move `describe("gitignore policy")` next to `gitignore.ts` (new `gitignore.test.ts`). Leave verify/slice/handoff in `engine.test.ts`. | 91 |
| SMELL-TB-08 | P3 | packages/semantic-engine/test/anchor-migration.test.ts:512 | Mixed LF/CRLF `.sem` source copied between two grammar `it`s (512–519 vs 532–539): same scalar / block-list / inline-list links, one with BOM. Target-binding `{ schemaVersion: 1, targetId: "checkout-v2", revision, artifactHash }` is restated in `model.test.ts:108`, `dsl.test.ts:133`, `engine.test.ts:263` — three layers, same shape, different hashes. | `"  link: [sym:function:src/a.ts:write:9, other:https://example.test/x]\\n",` | Grammar: `mixedForms(ref)` like `mixedEndings` already in the same file. Leave the three `checkout-v2` objects unless a shared typed builder appears for another reason. | 88 |

## Metrics
- Files read: 15 (all TB test files)
- Findings: 8 (P0: 0 / P1: 0 / P2: 7 / P3: 1)
- Partition test files: 15 (`core` 2, `semantic-model` 2, `semantic-dsl` 3, `semantic-engine` 8); 3032 loc
- Files >400 loc: 1 — `anchor-migration.test.ts` 1131 (next: `engine.test.ts` 293, `model.test.ts` 261, `link-diagnostics.test.ts` 259)
- 500+ line tests: 1 file; 0 `it` blocks ≥80 loc
- Functions / `it` >80 loc (approx): 0. Describe suites >80 loc: 7 — exclusive ownership 356, crash recovery 195, refinement-dsl 151, targets 138, atomicity 110, versioned model 103, gitignore 91
- `beforeEach`: 0 (no giant setup). `afterEach`: 3 small cleanups (`anchor-migration` 154, `targets` 27, `handoff-robust` 9)
- Copy-paste clusters: `facts()` ×3 engine files; `symbol()`/`RUN` ×2 packages; spawn `launch()` ×2; crash-point `for` ×2; gitignore policy ×3; empty node/change literals ×6+ in `model.test.ts`

## Recommendations
1. Split `anchor-migration.test.ts` first. It is the only TB god test and the only 1k-loc file; crash recovery and exclusive ownership are already named seams.
2. Stop inlining spawn/journal/two-file setup in that file — `crashMigration`, `rewritePreparedPath`, and a single `twoFileRepo()` already cover it.
3. Give `semantic-engine/test` one local `facts`/`report`/`model` helper and delete the copies in `determinism` and `verify-honesty`.
4. Add `node()`/`change()` in `model.test.ts` (copy the doctrine helpers, do not import across packages).
5. Pull gitignore cases out of `engine.test.ts`. Leave `core` / `path-safety` / `parse-fixes` / `handoff-robust` alone.
