# Semctx v0.3 delivery checkpoint

## Goal
Deliver reliable indexing and a source-backed continuation journey on the published v0.2.1 foundation.
## Scope
HOK-746 recovery; HOK-461/462/463/464 indexing baseline, partitioning, memory and auto mode; HOK-635 lifecycle; HOK-638/639/640/641 under HOK-636 for context applicability and continuation.
## Constraints
User approved the described v0.3 scope and starting order on 2026-09-13. Base: origin/main at 4f97c29748b00a45a3e9ac73d3bb20bc594da044. Worktree: C:/maintenence/semctx-v03, branch codex/v0.3-foundations. Preserve the dirty root checkout and all other worktrees. No provider removal, executor, cloud memory, fabricated measurements or weaker evidence/authority gates. Human outcomes remain unmeasured unless independently observed.
## Tasks
- [ ] Reproduce and repair current-schema stale-baseline recovery (HOK-746), preserving failed-rebuild evidence.
- [ ] Establish reproducible indexing baselines before optimization (HOK-461).
- [x] Accept a bounded context/proof applicability design before governed implementation (HOK-638; ADR 0027 accepted locally, issue acceptance still open).
- [ ] Reduce indexing memory and scope unsafe parallel fallback; calibrate measured auto mode (HOK-462/463/464).
- [ ] Prove index lifecycle across edits, branches, worktrees and interruption (HOK-635).
- [ ] Deliver the source-backed resumption dossier, applicability explanation and continuous journey (HOK-639/640/641; HOK-636).
- [ ] Validate the integrated candidate, document evidence/gaps, and reconcile Linear before release acceptance.
## Dependencies
Recovery precedes reliable indexing measurement. HOK-461 precedes optimization and lifecycle budgets. HOK-638 precedes HOK-639/640, which precede HOK-641. Independent discovery may run in parallel; one author per checkout.
## Done means
The scoped behavior, required negative/parity tests and canonical verify:pr pass on identified candidates; required cross-platform/real-repository measurements and independent review are observed. Publication, installed-host delivery and Linear Done are distinct and need their own evidence.
## Not-done risks
The user confirmed on 2026-09-13 that no Mac is available. The required lobby/Apple Silicon baseline stays NOT_MEASURED and HOK-461 remains open; a Windows synthetic run cannot replace that evidence. An index being fresh never validates historical evidence. A stale baseline may permit repair but never authorize an operation. Scope-shaping unresolved design stops dependent implementation, not independent work.
## Current state
Three isolated authoring candidates share base 4f97c29748b00a45a3e9ac73d3bb20bc594da044 and are now combined in C:/maintenence/semctx-v03 on codex/v0.3-foundations:

| Slice | Worktree | Observed local state |
| --- | --- | --- |
| HOK-746 recovery | C:/maintenence/semctx-v03 | Explicit index --record binds actual graph/claims/evidence, config, index snapshot and the semantic/lifecycle inputs read by controlStatus. An initial canonical run passed 2,236 tests with 9 existing skips. A later baseline A-B-A correction and stronger help/unknown assertions passed 26 targeted tests; both graph and baseline A-B-A witnesses were red before their guards and green after. The combined candidate requires fresh canonical verification. |
| HOK-461 benchmark | C:/maintenence/semctx-v03-bench | 33 targeted tests passed; 27 fresh Windows subprocess samples measured CPU and peak RSS and preserved all ten fingerprint components. The real-store corruption witness detects metadata drift hidden by graph/health equality. This small synthetic run had concurrent host work and is a harness smoke, not optimization evidence. Mac/lobby remains NOT_MEASURED. |
| HOK-638/639/640 continuation | C:/maintenence/semctx-v03-continuation | ADR 0027 accepted locally; CLI/MCP report implemented. Review corrections cover compatible diff hashes, staging-only drift, bounded descriptor reads, actual UTF-8 output budget and mandatory warnings. Positive and negative tests cover unchanged dirty state, committed/uncommitted drift, capture races, missing current index, long statements and mandatory-budget refusal. Acceptance requires combined tests and old Handoff v2 regressions. |

## Acceptance boundary

The first independent recovery audit returned PROOF_WEAK. Its concrete lifecycle A-B-A and insensitive help/unknown oracle findings were corrected. Native child review is instructional read-only, without an attested enforced filesystem sandbox; no schema-v3 proof-policy receipt or ALLOW exists. Fresh aggregate review and canonical checks still govern acceptance. Do not translate local tests or a local checkpoint commit into MERGE_READY, a published release, or Linear Done.

The aggregate audit found missing recovery guidance, outdated 38-tool test expectations and insufficient MCP/benchmark oracle coverage. Recovery diagnostics now name `semctx index --record` while preserving strict JSON output and actual verdicts. Catalogue tests explicitly require 39 tools and the new continuation tool. Transport tests call the registered server and packaged runtime, reject extra fields and relative roots, and compare the result with the application service. Benchmark tests use non-empty real claims and unresolved authored references to detect discarded fields. The first aggregate canonical run observed 2,306 passing tests, 9 existing skips and only the three outdated catalogue-count failures; a fresh full run is required after these corrections.

An owned detached checkout of Semctx indexed 4,366 nodes with explicit `apps/*/src/**/*.ts` and `packages/*/src/**/*.ts` selection. Initial recording, tracked dirty-source recovery and clean HEAD-drift recovery all returned PASS while retaining actual unknowns. This is a real-repository recovery smoke, not a performance measurement or an attestation of complete analysis. The first smoke wrapper used the unsupported `control status` spelling; those calls are excluded from proof, and the public `status --json` command is captured separately. HOK-641 still needs a replayable user journey and an independent continuity pilot.

No v0.3 release, performance improvement, human comprehension outcome, merge or installed-host delivery has been accepted. The new recovery worktree is not indexed in Semctx (REPOSITORY_NOT_INDEXED / UNSEALED); its local proof uses current source and executed tests rather than another worktree's cached index. Evidence and raw logs are retained outside the checkout in C:/Users/Hokli/.codex/state/semctx-v03-evidence.
