# Semctx v0.2 delivery checkpoint

## Goal
Deliver the complete v0.2 first-use and contributor journey, reproducible public-change replay and exact release delivery proof.
## Scope
HOK-629/632/633/634/645: demo, impact/adoption pilot, public evidence page, voluntary local feedback. HOK-637/642/643/644: clean contributor setup, targeted loop, shareable diagnostics. Verify OMP proposals separately before choosing its integration.
## Constraints
User confirmed v0.2 on 2026-09-08, then explicitly waived human participants: "rien de prevu et on va s'en passer". Adoption, retention, human comprehension and contributor time remain NOT_MEASURED; recruitment and fourteen-day follow-up are removed from release gates. Baseline: v0.1.20, `9d33af507392b5fe563cc3dfd72a321fc0db4372`. Integration worktree: `C:/maintenence/semctx-v0.2`, branch `codex/v0.2-adoption`. Preserve other checkouts, stable and existing enforcement. No new dependencies, telemetry, executor or provider replacement. Codex owns decisions/review/release; one bounded author per isolated worktree.
## Tasks
- [x] Build the packaged three-case demo and readable report (HOK-632); final independent aggregate review remains below.
- [x] Build pilot protocol, identity-bound measurements and voluntary local feedback (HOK-633/645).
- [x] Build contributor setup diagnostics, targeted proof and private-safe diagnostics (HOK-637/642/643/644); release delivery remains below.
- [x] Verify the conflicting OMP proposals in isolation; decide the smallest compatible delivery.
- [ ] Publish the reviewed public demo/evidence bundle with explicit limitations (HOK-634).
- [ ] Freeze the packaged candidate; obtain cross-platform CI and independent proof review.
- [x] Replay thirty frozen public changes across three repositories; retain failures and unknown labels without claiming human or accuracy measurements.
- [ ] Reconcile findings, publish v0.2 through the existing tag pipeline, regenerate release demo and prove both installed hosts.
## Dependencies
Demo and measurement tooling precede frozen-candidate replay. Stable publication follows accepted candidate evidence; changed behavior invalidates affected measurements. Unadjudicated labels prohibit accuracy scoring.
## Done means
Versioned runnable journey, raw results with source/artifact identities, honest replay conclusions, reviewed PRs, canonical verification/CI, public release/npm/stable, host delivery and issue read-back. Missed thresholds may yield a negative verdict; waived human studies remain unmeasured.
## Not-done risks
Corpus labels have no independent adjudication and stay UNKNOWN. No outreach is authorized. The replay establishes execution/report integrity only; release readiness is not yet claimed. Thirty selected public changes have verified Git pairs, exact paths and MIT licenses; the final execution protocol is frozen.
## Current state
The maintainer explicitly switched authoring/review to Codex only because Claude quota is low.
All task-owned Claude processes were stopped on 2026-09-08; partial work was preserved. Native
Codex now owns every remaining tranche, including independent review. This instruction overrides
the prior default delegation ratio for this delivery; do not launch Claude model calls on resume.

Demo, voluntary reports, contributor checks and the public page are integrated locally. Product
manifests are 0.2.0; publication has not occurred. Targeted tests and TypeScript/ESLint pass on the
respective slices; they are not the aggregate gate. The golden demo now builds its runtime in its
own temporary directory so tests do not replace a concurrently selected CLI artifact.

The first public replay completed: 30 observed, no infrastructure failure, 29 trusted reports and
one diff mismatch. All labels are UNKNOWN and scores are null. The real ts-pattern PR-332 diff
deletes tests/records.test.ts; the historical parser deliberately omitted deleted files. A bounded
parser/report correction and an explicit untrustedCases summary count are integrated.
Keep the first experiment, impact-pilot-research-20260908010755-f9ff5989, immutable.
Its protocol digest is sha256:136ba9896d708c3834c80890648731db6546a22fae9824b38a0c46781da79f35.

The final candidate replay completed on 2026-09-08: 30 observed, 30 trusted, zero failed and zero
untrusted reports. The deleted-file case now matches all twelve Git paths. Experiment
impact-pilot-research-20260908013517-64a9a957 binds protocol
sha256:eca8bfa7d0338a054a7e432db9b2fc92d61712fa8bf3fcabba4d03542a467df9.
All thirty labels remain UNKNOWN, scores remain null and the verdict remains EVIDENCE_MISSING.
See ../pilot/results/v0.2-candidate.md for both attempts and artifact identities.

OMP standard integration through the existing tag-pinned git-subdir catalogue is accepted in
ADR0020. The last correction preserves the original session cwd and maps OMP filesystem aliases;
unresolved internal URL Git scopes fail closed unless explicitly disabled. Native review also
corrected report-path resolution and loaded feedback ID binding. Interrupted feedback writer locks
require documented manual recovery after checking that no writer remains; no automatic reclamation.

The final packaged demo completed with WARN, three of three expected observations and no unknown
report item. The public candidate projection includes this demo and the final replay. The first
local canonical run completed with four test timeouts under concurrent host load; it is not green.
The first Linux/macOS CI runs exposed in-process Bun compilation corrupting later workspace
resolution. The golden demo now compiles in a child process as required by the existing plugin
build contract; its temporary outputs stay isolated. The demo/benchmark pair passes 19 tests on
Windows, and tooling TypeScript/ESLint pass. Cross-platform confirmation and fresh aggregate proof
review remain, followed by merge/tag/npm/stable/host delivery and additive Pages /demo/ publication.
The correction changes only the test harness; the captured CLI entry remains byte-identical.
No release-readiness claim exists yet.
HOK-585 historical compatibility remains deferred and does not block the tested host baseline.
