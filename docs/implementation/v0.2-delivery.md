# Semctx v0.2 delivery checkpoint

## Goal
Deliver the complete v0.2 first-use and contributor journey, reproducible public-change replay and exact release delivery proof.
## Scope
HOK-629/632/633/634/645: demo, impact/adoption pilot, public evidence page, voluntary local feedback. HOK-637/642/643/644: clean contributor setup, targeted loop, shareable diagnostics. Verify OMP proposals separately before choosing its integration.
## Constraints
User confirmed v0.2 on 2026-09-08, then explicitly waived human participants: "rien de prevu et on va s'en passer". Adoption, retention, human comprehension and contributor time remain NOT_MEASURED; recruitment and fourteen-day follow-up are removed from release gates. Baseline: v0.1.20, `9d33af507392b5fe563cc3dfd72a321fc0db4372`. Integration worktree: `C:/maintenence/semctx-v0.2`, branch `codex/v0.2-adoption`. Preserve other checkouts, stable and existing enforcement. No new dependencies, telemetry, executor or provider replacement. Codex owns decisions/review/release; one bounded author per isolated worktree.
## Tasks
- [x] Build the packaged three-case demo and readable report (HOK-632); final independent aggregate review remains below.
- [x] Build pilot protocol, identity-bound measurements and voluntary local feedback (HOK-633/645).
- [x] Build contributor setup diagnostics, targeted proof and private-safe diagnostics (HOK-637/642/643/644); release delivery remains below.
- [x] Verify the conflicting OMP proposals in isolation; decide the smallest compatible delivery.
- [ ] Publish the reviewed public demo/evidence bundle with explicit limitations (HOK-634).
- [ ] Freeze the packaged candidate; obtain cross-platform CI and independent proof review.
- [x] Replay thirty frozen public changes across three repositories; retain failures and unknown labels without claiming human or accuracy measurements.
- [ ] Reconcile findings, publish v0.2 through the existing tag pipeline, regenerate release demo and prove both installed hosts.
## Dependencies
Demo and measurement tooling precede frozen-candidate replay. Stable publication follows accepted candidate evidence; changed behavior invalidates affected measurements. Unadjudicated labels prohibit accuracy scoring.
## Done means
Versioned runnable journey, raw results with source/artifact identities, honest replay conclusions, reviewed PRs, canonical verification/CI, public release/npm/stable, host delivery and issue read-back. Missed thresholds may yield a negative verdict; waived human studies remain unmeasured.
## Not-done risks
Corpus labels have no independent adjudication and stay UNKNOWN. No outreach is authorized. The replay establishes execution/report integrity only; release readiness is not yet claimed. Thirty selected public changes have verified Git pairs, exact paths and MIT licenses; the final execution protocol is frozen.
## Current state
Snapshot before final release acceptance, 2026-09-08. Product versions are 0.2.0; npm/stable/Pages
publication remains pending. The maintainer requires Codex-only authoring and review because
Claude quota is low. No Claude model call is permitted; metadata-only host installation checks
remain part of the existing two-host delivery proof.

Demo, reports, contributor checks, OMP and the public page are integrated. Functional cross review
corrected lossy JSON identity, feedback source binding/chronology/concurrency diagnostics, SemVer
support, contradictory public projections, empty-test false success, malformed pilot reports and
unsafe output paths. The collector binds the full runtime used to validate its observations.
TypeScript, ESLint, plugin parity and focused regression tests pass. Candidate af4e906 completed
all 2,095 tests on each of three operating systems (CI run 34190499529). Its independent audit
identified an OMP unknown-enablement fallback, incomplete finding attribution, missing raw demo
capture identities and a preview/output conflict. These are corrected: unknown enablement blocks
after evaluator failure, findings use both node identities and locations, unassigned findings
block completion, saved stdout/stderr have content digests, and preview rejects output flags.
The prior 59 negative witnesses and CI remain historical; this complete candidate requires its
own frozen epoch, fresh aggregate audit and canonical CI. Candidate 6b2eee4 passed Ubuntu and
Windows CI; macOS exposed a temporary-directory alias in one new test fixture. The fixture now
uses its canonical path, preserving the production output-path checks. The 6b2eee4 aggregate audit
also found overly broad demo completion, missing process timing/signal evidence, permissive public
SemVer validation and an inaccurate fixture snippet. These are corrected: completion requires the
exact frozen advisory WARN, process records retain measured duration and termination signal, and
the public projection checks exact rule multiplicity, version syntax and fixture text.

Candidate d9a0a40 then passed canonical CI on Windows, Ubuntu and macOS (run 34227116548,
2,109 tests collected on each platform) and 71 freshly executed negative witnesses. Its aggregate
audit identified two remaining pilot input/coherence defects. Local source paths now reject
relative inputs and are fully resolved once; offline reporting binds all three candidate commands
to their expected stage, entry, executable, range and disposable root. All 82 pilot tests pass.
The auditor withdrew a separate cross-run fixture-commit equality finding after checking ADR0018:
the required invariant is stable Git identity within each run, with frozen content across runs.
The d9a0a40 audit, witnesses and CI remain historical; this corrected candidate still requires its
own aggregate audit, witness markers and exact-SHA CI before release acceptance.

Candidate 93c16b4 passed the full Windows CI gate (2,113 tests collected). Linux and macOS each
failed only the Windows-specific path in the prototype-key test fixture; its absolute paths are
now portable while the own-key and null-prototype assertions remain. The aggregate audit found
that late demo blocks discarded valid product observations and that the public builder accepted
incomplete completed manifests. These are corrected together: blocked records retain verdicts,
unknowns and complete attributed/unassigned findings, including when a source-identity failure
takes precedence. The public builder validates command, runtime and frozen-fixture coherence and
preserves the blocked state. Optional worker absence remains distinct from an incomplete identity.
The 65 combined demo/public tests, tooling typecheck and scoped lint pass on the integrated fixes.
The 35 repository and three policy witnesses executed for 93c16b4 remain historical; that candidate
was not accepted. A fresh aggregate audit and complete final witness set are still required.

Candidate 0f7bd29 then passed exact-SHA CI run 34239539892, collecting 2,120 tests on Windows,
Ubuntu and macOS, and all 77 fresh negative witnesses passed. Its fresh aggregate audit still found
two public-reporting defects: public exports could disclose arbitrary case/repository identifiers
and critical-file paths, and adjudicated reports lacked per-repository comparative scores. Those
findings were corrected in source candidate 259146d. The complete pilot suite passes 85 tests; two
separate targeted mutation checks each observed RED then GREEN for the corrected privacy projection
and repository scoring. The 0f7bd29 CI, witnesses and audit remain historical;
they do not authorize the corrected source candidate.

Two clean local full contributor runs (af4e906 and 6b2eee4) hit the same three existing Git-guard
test timeouts. The second run reproduced the failures without concurrent witness execution, so
contention alone is not an established explanation. A redundant Git-context resolution was removed
without changing guard semantics; targeted checks pass, but a completed full-run speed improvement
has not been established. No timeout or gate was weakened. Final canonical CI and independent
acceptance remain required, and the failed local runs remain evidence.

The packaged demo completed with WARN, all three expectations and no unknown report item. All
eight saved stdout/stderr captures were independently rehashed against the manifest. These digests
identify bytes at capture time; COMPLETED does not monitor later edits. Runtime bytes remain
unchanged. The demo was recaptured after the blocked-record and public-input corrections; its
three frozen observations match and its findings retain their full local metadata. The ninth public
replay against source candidate 259146d observed 30 trusted cases, zero failed
or untrusted reports, and exact Git identities including all merge-bases/ranges and the deleted
ts-pattern file. Experiment `impact-pilot-research-20260908155338-3d9cddfa` binds
`sha256:1eaab6944d8d310ca0ed09a031bb1c4ac8a50eaf49714eb6e3598515895501df`. Every label stays UNKNOWN,
global and per-repository scores stay null, and the research verdict remains EVIDENCE_MISSING.
The public projection uses experiment-local identifiers and exposes no repository path, original
case identity or critical-file path. Candidate and runner identities match all 103 and 610 frozen
files respectively; an independent check revalidated 30 Git identities, 90 command-output digests
and 60 baseline input/output digests with no error. All eight preceding attempts remain separately identified in
../pilot/results/v0.2-candidate.md. Human studies and fourteen-day follow-up are waived.

The site now rejects incompatible evidence before changing observation fields. Its loaded empty,
blocked, pilot-only, release and scored states are tested; real no-JavaScript, mobile and keyboard
checks preserve useful fixture content and explicit limitations. The proof replay driver also
records observed clean Git HEAD/status before and after every disposable-clone mutation. No old
receipt or old-epoch witness marker is accepted as final authorization.

OMP uses the existing tag-pinned git-subdir catalogue and Agent-Plugins standard (ADR0020).
The adapter preserves session cwd and OMP path/environment semantics. OMP 18.1.11 exposes raw
internal URLs to the extension before Bash expansion, so unresolved internal Git scopes use only
the known filesystem session root to determine enablement: advisory remains non-blocking, while an
enabled, unknown or failed evaluation fails closed unless explicitly disabled. Target-only opt-in
behind an opaque URL is not discoverable at this boundary. OMP remains experimental and outside
stable host attestation; its final catalogue install/upgrade/uninstall must be observed after the
tag exists. A candidate installation on OMP 18.1.11 exercised installation, skill discovery, MCP,
the shim, guard, upgrade and uninstall against a checked branch commit. It also revealed that OMP
may select an ancestor `.omp` directory before the Git root. The documented recipe now creates
the project's `.omp` directory before installation; a behavioral check executes this command and
verifies the real upstream resolver. One earlier probe created a run-owned entry in the real
profile. Its registry entry, lock entry and package junction were archived and removed with an
exact allowlist. There was no prior snapshot of unrelated profile files; full-profile restoration
is therefore not claimed. Subsequent probes use disposable profiles and compare targeted real
profile snapshots before and after execution. Immutable release-tag installation is still pending.

Remaining delivery: freeze this complete candidate, obtain its exact-SHA CI and fresh independent
proof receipt, merge, publish through the annotated-tag workflow, read npm/stable/GitHub Release,
validate both installed hosts, replay the downloaded npm demo, and publish only gh-pages/demo/.
No release-readiness or publication claim is made in this snapshot. HOK-585 earliest historical
host compatibility remains deferred and does not block the tested host baseline.
