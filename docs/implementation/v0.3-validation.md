# v0.3 local validation checkpoint

This evidence supersedes the pre-validation status notes in `v0.3-delivery.md`. It is a local implementation checkpoint, not release acceptance.

## Candidate and validation

Base and pre-commit HEAD: `4f97c29748b00a45a3e9ac73d3bb20bc594da044`, branch `codex/v0.3-foundations`. The 59 changed paths were frozen as `sha256:c7380dfbc75bc1bdd78d137b717d826e71e9b9a56093d8b5018795d7f98a623e`. All path digests and the epoch remained unchanged through verification. This evidence note was added afterwards and receives a separate documentation check.

`bun run verify:pr -- --base 4f97c29748b00a45a3e9ac73d3bb20bc594da044` exited 0 on Windows/Bun 1.4.0: **2,313 passed, 9 existing skips, 0 failed**, 12,498 expectations across 166 files. Types, quality checks, documentation and generated plugin parity passed. Both host bundles are byte-identical. The earlier 38-versus-39 catalogue failures were corrected by explicit count and tool-name assertions.

Seven current-candidate fault injections each failed before restoration and passed afterwards: consumed graph identity, consumed baseline identity, recovery guidance, returned claims, returned diagnostics, MCP registration and strict input. Real in-memory and packaged MCP calls establish positive parity and rejection of extra fields and relative repository roots.

## Observed behavior

HOK-746: ordinary indexing preserves stale recorded evidence; `index --record` rebuilds, verifies and atomically records the actual result, including unknowns and BLOCK. A detached Semctx checkout with explicit apps/packages source selection indexed 4,366 nodes. Dirty-source and clean HEAD-drift recovery completed without manually deleting the evidence baseline; the final public `status --json` returned FRESH. Initial smoke calls mistakenly used `control status`; those rejected calls are excluded from evidence.

HOK-461: the final integrated candidate produced 27 fresh Windows subprocess samples, with CPU and native peak RSS measured in all samples and all ten logical fingerprint components equivalent within each corpus. Small synthetic corpora and concurrent host work make this a harness smoke. It is not a performance baseline or a Mac result.

HOK-639/640/641: the public CLI journey on Semctx connected source inspection, explicit bindings, a plan, reconciliation, capsule capture and applicability explanation. Task `task:dc537463` retained source coordinate `repo:sym:function:packages/app-services/src/index-recovery.ts:recoverIndexEvidence` and capsule `sha256:2ab725d6ed618d140667cb185134965135775762da5b559d2c86f2418e980e82`. After a controlled comment edit, the diff dependency changed from APPLICABLE to STALE; resume refused with HANDOFF_DIFF_STALE. Historical capsule bytes were unchanged, and the source bytes were restored. The pre-edit plan remained VIOLATED because its expected edit was absent; capture never promoted it to a successful proof.

The recipe required explicit authored-link resolution, a concrete edit expectation and a task-relevant mapped semantic progress coordinate. Unmapped or unrelated progress coordinates were refused. The ignored artifacts and replay scripts remain in the local evidence directory; a portable host recipe and independent user pilot remain open.

## Remaining work and limits

The real explanation took about 205 seconds unchanged and 157 seconds after the edit, while status took about 104 seconds. These are observational costs on a busy Windows host, not controlled comparisons. HOK-635 should measure the individual capture/status phases before any optimization; stability observations and freshness checks must remain intact.

Mac/lobby is unavailable and NOT_MEASURED. HOK-462/463/464 optimization and HOK-635 budgets retain their baseline dependency. Independent comprehension/continuity evidence remains NOT_MEASURED. No speed, adoption, proof-reuse authority, provider replacement or executor claim is made.

Independent source review found no remaining actionable defect in the corrected areas. Formal proof remains PROOF_WEAK because no enforced read-only sandbox attestation or proof-policy ALLOW exists. No CI, merge, tag, npm publication, stable promotion or installed-host delivery was performed.

Raw logs, epoch identities, exact fault injections, benchmark JSON, replay inputs and checksums are retained in `C:/Users/Hokli/.codex/state/semctx-v03-evidence/local-checkpoint-evidence.json` and its neighboring files. The original dirty checkout at `C:/maintenence/semctx` remains on `fix/hok-662-p0-security` at `23b49dc70d666b5b76d4333dadb4bbdf6070f077`.
